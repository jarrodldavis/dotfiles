# Commit Identity
[user]
    name = Jarrod Davis
    email = developer@jarrodldavis.com
[gpg "ssh"]
    allowedSignersFile = ~/.ssh/allowed_signers
[include]
    path = ~/.gitconfigs/bazzite
    path = ~/.gitconfigs/macos
    path = ~/.gitconfigs/ssh

[includeIf "hasconfig:remote.origin.url:git@github.com:*/**"]
    path = ~/.gitconfigs/github-origin
[includeIf "hasconfig:remote.origin.url:https://github.com/**"]
    path = ~/.gitconfigs/github-origin
[includeIf "hasconfig:remote.upstream.url:git@github.com:*/**"]
    path = ~/.gitconfigs/github-upstream
[includeIf "hasconfig:remote.upstream.url:https://github.com/**"]
    path = ~/.gitconfigs/github-upstream

# Diffs
[core]
    pager = delta
[interactive]
    diffFilter = delta --color-only
[merge]
    conflictstyle = diff3
[diff]
    algorithm = histogram
    colorMoved = default
[delta]
    navigate = true
    light = false
    side-by-side = true

# Other Settings
[core]
    excludesfile = ~/.gitignore
[init]
    defaultBranch = main
[pull]
    rebase = false
[fetch]
    all = true
[push]
	followTags = true
